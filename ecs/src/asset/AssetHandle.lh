/*
 *  A handle to an asset managed by the AssetMgr.
 */

#include "iro/common.h"
#include "iro/traits/nil.h"

/* ============================================================================
 */
struct AssetHandle
{
  u32 id = 0;
};

DefineNilValue(AssetHandle, {0}, { return x.id == 0; });
