/*
 *  Central system for game input.
 *
 *  This is currently just a mirror of the input stored on the Engine,
 *  but will eventually be a separate input state.
 */

$ require "common"

@@lpp.import "game/entity/EntitySystem.lh"

struct InputMgr;

/* ============================================================================
 */
struct InputSys : EntitySystem
{
  InputMgr* mgr;

  b8 init() { return true; }
};
