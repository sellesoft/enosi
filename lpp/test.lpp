$$$

DEBUG = true

local import = function(s)
	return lpp.parse_file(s)
end

local values =
{
	100,
	200,
	300,
	400
}

$$$

@import "apple.lpp"

int main()
{
	static const int values[] =
	{
		$ for i=1,#values do
			$(values[i]),
		$ end
	}
}
